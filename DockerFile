# Use the official Python base image
FROM python:3.10-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the requirements file and install dependencies
# We copy requirements.txt separately to leverage Docker caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Create the directories for source code and model artifacts
RUN mkdir -p src/tests models

# Copy the entire source code and test files
COPY src/ src/

# The model must be present for the app to start up.
# This file is generated by running src/model_pipeline.py
COPY models/best_model_pipeline.pkl models/

# Expose the port the API will run on
EXPOSE 8000

# Command to run the application using Uvicorn
# The app is located at src/app.py, and the FastAPI instance is named 'app'
CMD ["uvicorn", "src.app:app", "--host", "0.0.0.0", "--port", "8000"]